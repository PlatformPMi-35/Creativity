<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="New" Command="{Binding NewFile}"/>
                <MenuItem Header="Open" Command="{Binding OpenFile}"/>
                <MenuItem Header="Save" Command="{Binding SaveFile}"/>
            </MenuItem>
            <MenuItem Header="Shapes"/>
        </Menu>
        <Canvas x:Name="canvasDrawingArea" Background="#FFFFFE" DockPanel.Dock="Left" MouseLeftButtonDown="canvasDrawingArea_MouseLeftButtonDown" MouseLeftButtonUp="canvasDrawingArea_MouseLeftButtonUp" MouseMove="canvasDrawingArea_MouseMove"/>
    </DockPanel>
</Window>
